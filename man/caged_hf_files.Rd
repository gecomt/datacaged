% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/availability.R
\encoding{UTF-8}
\name{caged_hf_files}
\alias{caged_hf_files}
\alias{caged_ftp_files}
\title{List available competencies in the HuggingFace repository}
\usage{
caged_hf_files(type = "novo", n = 12, timeout = 15, verbose = TRUE)

caged_ftp_files(type = "novo", n = 12, timeout = 15, verbose = TRUE)
}
\arguments{
\item{type}{character. Which series to query:
\code{"novo"} (Novo CAGED, 2020+), \code{"antigo"} (CAGED antigo, até 2019)
ou \code{"ajustes"} (CAGED Ajustes, série histórica de correções).
Default: \code{"novo"}.}

\item{n}{integer. Number of most recent competencies to return.
Use \code{Inf} to return all. Default: \code{12}.}

\item{timeout}{integer. Timeout in seconds. Default: 15.}

\item{verbose}{logical. If TRUE, displays a table in the console. Default: TRUE.}
}
\value{
invisible tibble with columns \code{competencia} (AAAAMM),
\code{ano}, \code{mes} e \code{url}, ordenado do mais recente para o mais antigo.
}
\description{
Queries the HuggingFace API and returns available months
for download, ordered from most recent to oldest.

\code{caged_ftp_files()} é um alias mantido por compatibilidade reversa —
emite um aviso pedindo migração para \code{caged_hf_files()}.
}
\examples{
\donttest{
# Últimos 12 meses disponíveis (padrão)
caged_hf_files()

# Todos os meses do Novo CAGED
caged_hf_files(n = Inf)

# CAGED antigo
caged_hf_files(type = "antigo")

# CAGED Ajustes
caged_hf_files(type = "ajustes")

# Usar o resultado para baixar automaticamente o mês mais recente
disp <- caged_hf_files(n = 1, verbose = FALSE)
caged_load(
  years    = disp$ano,
  months   = disp$mes,
  db_path = file.path(tempdir(), "caged.duckdb")
)
}

}
\seealso{
\code{\link[=caged_status]{caged_status()}} para verificar se o repositório está acessível.

\code{\link[=caged_load]{caged_load()}} para baixar após identificar as competências.
}
