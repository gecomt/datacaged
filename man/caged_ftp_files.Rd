% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/availability.R
\name{caged_ftp_files}
\alias{caged_ftp_files}
\title{Lista as competências disponíveis no FTP do MTE}
\usage{
caged_ftp_files(type = "novo", n = 12, timeout = 15, verbose = TRUE)
}
\arguments{
\item{type}{character. Qual série consultar:
\code{"novo"} (Novo CAGED, 2020+), \code{"antigo"} (CAGED antigo, até 2019)
ou \code{"ajustes"} (CAGED Ajustes, série histórica de correções).
Default: \code{"novo"}.}

\item{n}{integer. Número de competências mais recentes a retornar.
Use \code{Inf} para retornar todas. Default: \code{12}.}

\item{timeout}{integer. Timeout em segundos. Default: 15.}

\item{verbose}{logical. Se TRUE, exibe table no console. Default: TRUE.}
}
\value{
tibble invisível com colunas \code{competencia} (AAAAMM),
\code{ano}, \code{mes} e \code{url}, ordenado do mais recente para o mais antigo.
}
\description{
Consulta o servidor FTP do MTE e retorna os meses disponíveis
para download, ordenados do mais recente para o mais antigo.
}
\examples{
\dontrun{
# Últimos 12 meses disponíveis (padrão)
caged_ftp_files()

# Todos os meses do Novo CAGED
caged_ftp_files(n = Inf)

# CAGED antigo
caged_ftp_files(type = "antigo")

# CAGED Ajustes
caged_ftp_files(type = "ajustes")

# Usar o resultado para baixar automaticamente o mês mais recente
disp <- caged_ftp_files(n = 1, verbose = FALSE)
caged_load(
  years    = disp$ano,
  months   = disp$mes,
  db_path = "caged.duckdb"
)
}

}
\seealso{
\code{\link[=caged_status]{caged_status()}} para verificar se o FTP está online.

\code{\link[=caged_load]{caged_load()}} para baixar após identificar as competências.
}
