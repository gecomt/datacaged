% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjustments.R
\name{caged_adjustments_load}
\alias{caged_adjustments_load}
\title{Pipeline completo para o CAGED Ajustes}
\usage{
caged_adjustments_load(
  years,
  months = seq_len(12L),
  db_path,
  destdir = NULL,
  force_download = FALSE,
  overwrite_competencies = FALSE,
  timeout = 300
)
}
\arguments{
\item{years}{integer vector. Desired years. Maximum: 2019.}

\item{months}{integer vector. Desired months (1–12). Default: \code{seq_len(12L)}.}

\item{db_path}{character. Path to the file \code{.duckdb}.}

\item{destdir}{character or NULL. Cache directory for \verb{.7z} files.
Default: \code{tools::R_user_dir("datacaged", "cache")}.}

\item{force_download}{logical. If TRUE, re-downloads even if already cached.
Default: FALSE.}

\item{overwrite_competencies}{logical. If TRUE, overwrites competencies
already in the database. Default: FALSE.}

\item{timeout}{integer. Timeout in seconds. Default: 300.}
}
\value{
invisível: tibble com estatísticas do banco após a carga.
}
\description{
Baixa, parseia e grava os arquivos do CAGED Ajustes no banco DuckDB local.
}
\details{
O CAGED Ajustes contém correções retroativas de vínculos do CAGED antigo
(até 2019). Cada arquivo \verb{CAGEDAJUSTES_\{MM\}\{AAAA\}.7z} registra
movimentações ajustadas após a declaração original — essencial para
reconstrução de séries históricas mais precisas.

Os dados são gravados na tabela \code{caged_ajustes} do banco DuckDB, com o mesmo
schema do \code{caged_antigo}.
}
\examples{
\donttest{
# List available adjustment competencies first
avail <- caged_hf_files(type = "ajustes", n = 3, verbose = FALSE)

if (nrow(avail) > 0) {
  # Download and load adjustments
  caged_adjustments_load(
    years   = avail$ano[1],
    months  = avail$mes[1],
    db_path = file.path(tempdir(), "caged.duckdb")
  )

  con <- caged_connect(file.path(tempdir(), "caged.duckdb"))
  dplyr::tbl(con, "caged_ajustes") |>
    dplyr::group_by(competencia) |>
    dplyr::summarise(saldo = sum(saldomovimentacao, na.rm = TRUE)) |>
    dplyr::collect()
  DBI::dbDisconnect(con, shutdown = TRUE)
}
}

}
\seealso{
\code{\link[=caged_load]{caged_load()}} para Novo CAGED (2020+) e CAGED antigo (até 2019).

\code{\link[=caged_hf_files]{caged_hf_files()}} com \code{type = "ajustes"} para listar disponibilidade.
}
