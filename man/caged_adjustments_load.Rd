% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjustments.R
\encoding{UTF-8}
\name{caged_adjustments_load}
\alias{caged_adjustments_load}
\title{Full pipeline for CAGED Adjustments}
\usage{
caged_adjustments_load(
  years,
  months = seq_len(12L),
  db_path,
  destdir = NULL,
  force_download = FALSE,
  overwrite_competencies = FALSE,
  timeout = 300
)
}
\arguments{
\item{years}{integer vector. Desired years. Maximum: 2019.}

\item{months}{integer vector. Desired months (1-12).
Default: \code{seq\_len(12L)} (todos os meses).}

\item{db_path}{character. Path to the file \code{.duckdb}.}

\item{destdir}{character or NULL. Diretório de cache dos \verb{.7z}.
Default: \code{tools::R_user_dir("datacaged", "cache")}.}

\item{force_download}{logical. Se TRUE, re-baixa mesmo se já em cache.
Default: FALSE.}

\item{overwrite_competencies}{logical. Se TRUE, regrava competências
já existentes no banco. Default: FALSE.}

\item{timeout}{integer. Timeout in seconds. Default: 300.}
}
\value{
invisible: tibble com estatísticas do banco após a carga.
}
\description{
Baixa, parseia e grava os arquivos do CAGED Ajustes no banco DuckDB local.
}
\details{
O CAGED Ajustes (\verb{/pdet/microdados/CAGED_AJUSTES}) contém correções retroativas
de vínculos do CAGED antigo (até 2019). Cada arquivo \verb{CAGEDAJUSTES_\{MM\}\{AAAA\}.7z}
registra movimentações ajustadas após a declaração original — essencial para
reconstrução de séries históricas mais precisas.

Os dados são gravados na tabela \code{caged_ajustes} do banco DuckDB, com o mesmo
schema do \code{caged_antigo}.
}
\examples{
\donttest{
# Baixar ajustes de 2019
caged_adjustments_load(
  years   = 2019,
  months  = seq_len(12L),
  db_path = file.path(tempdir(), "caged.duckdb")
)

# Série completa de ajustes
caged_adjustments_load(
  years   = 2010:2019,
  db_path = file.path(tempdir(), "caged.duckdb")
)

# Listar competências disponíveis antes de baixar
caged_hf_files(type = "ajustes")

# Consultar após carregar
con <- caged_connect(file.path(tempdir(), "caged.duckdb"))
dplyr::tbl(con, "caged_ajustes") |>
  dplyr::group_by(competencia) |>
  dplyr::summarise(
    saldo       = sum(saldomovimentacao, na.rm = TRUE),
    n_registros = dplyr::n()
  ) |>
  dplyr::arrange(competencia) |>
  dplyr::collect()
DBI::dbDisconnect(con, shutdown = TRUE)

# Comparar antigo vs ajustes (saldo líquido ajustado)
con <- caged_connect(file.path(tempdir(), "caged.duckdb"))
antigo  <- dplyr::tbl(con, "caged_antigo")  |>
  dplyr::group_by(competencia) |>
  dplyr::summarise(saldo_original = sum(saldomovimentacao, na.rm = TRUE))
ajustes <- dplyr::tbl(con, "caged_ajustes") |>
  dplyr::group_by(competencia) |>
  dplyr::summarise(saldo_ajuste = sum(saldomovimentacao, na.rm = TRUE))
dplyr::full_join(antigo, ajustes, by = "competencia") |>
  dplyr::mutate(saldo_final = saldo_original + saldo_ajuste) |>
  dplyr::collect()
DBI::dbDisconnect(con, shutdown = TRUE)
}

}
\seealso{
\code{\link[=caged_load]{caged_load()}} para Novo CAGED (2020+) e CAGED antigo (até 2019).

\code{\link[=caged_hf_files]{caged_hf_files()}} com \code{type = "ajustes"}
para listar disponibilidade.
}
