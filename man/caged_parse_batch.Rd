% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\encoding{UTF-8}
\name{caged_parse_batch}
\alias{caged_parse_batch}
\title{Read and normalise multiple CAGED .7z files}
\usage{
caged_parse_batch(paths, .progress = TRUE)
}
\arguments{
\item{paths}{character vector. Caminhos dos arquivos \verb{.7z}.}

\item{.progress}{logical. Exibe barra de progresso. Default: TRUE.}
}
\value{
tibble empilhado com todos os registros, ou NULL se nenhum
arquivo for legível.
}
\description{
Wrapper sobre \code{caged_parse()} que processa um vetor de caminhos,
empilha os resultados e reporta progresso.
}
\details{
Tipicamente você não chama esta função diretamente — ela é usada
internamente por \code{caged_load()}. Mas é útil quando você quer controle
manual sobre o que parsear.
}
\examples{
\donttest{
# Ler todos os arquivos MOV de um diretório de cache
cache <- tools::R_user_dir("datacaged", "cache")
arquivos_mov <- list.files(
  file.path(cache, "caged_novo", "2023"),
  pattern    = "CAGEDMOV",
  full.names = TRUE
)
df <- caged_parse_batch(arquivos_mov)

# Gravar no banco após parsear
caged_to_duckdb(df, db_path = file.path(tempdir(), "caged.duckdb"))
}

}
\seealso{
\code{\link[=caged_parse]{caged_parse()}} para processar um arquivo individual.

\code{\link[=caged_to_duckdb]{caged_to_duckdb()}} para gravar o resultado no banco.
}
