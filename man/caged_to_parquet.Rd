% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{caged_to_parquet}
\alias{caged_to_parquet}
\title{Exporta tabelas do banco DuckDB para arquivos Parquet}
\usage{
caged_to_parquet(
  db_path,
  output_dir,
  tables = NULL,
  partition_by = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{db_path}{character. Path to the file \code{.duckdb}.}

\item{output_dir}{character. Output directory for \code{.parquet} files.
Criado automaticamente se não existir.}

\item{tables}{character vector or NULL. Tables to export. If NULL,
exporta todas as tabelas CAGED presentes no banco.}

\item{partition_by}{character or NULL. Column for partitioning
(ex: \code{"uf"} ou \code{"competencia"}). If NULL, generates one file per table.}

\item{overwrite}{logical. Overwrite existing files. Default: FALSE.}
}
\value{
invisível: tibble com \code{tabela}, \code{arquivo} e \code{registros} exportados.
}
\description{
Usa o mecanismo nativo \verb{COPY TO ... (FORMAT PARQUET)} do DuckDB para
exportação eficiente. Significativamente mais rápido que \code{collect()} +
\code{arrow::write_parquet()} para grandes volumes.
}
\examples{
\donttest{
db <- file.path(tempdir(), "caged.duckdb")
caged_load(years = 2023, months = 1, db_path = db)

# Exportar todas as tabelas
caged_to_parquet(db, output_dir = tempdir())

# Exportar só caged_mov, particionado por UF
caged_to_parquet(
  db,
  output_dir   = tempdir(),
  tables       = "caged_mov",
  partition_by = "uf"
)
}

}
\seealso{
\code{\link[=caged_info]{caged_info()}} para listar tabelas disponíveis.
}
