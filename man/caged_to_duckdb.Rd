% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{caged_to_duckdb}
\alias{caged_to_duckdb}
\title{Grava um data.frame de microdados do CAGED em uma tabela DuckDB}
\usage{
caged_to_duckdb(
  df,
  db_path = NULL,
  table = NULL,
  overwrite_competencies = FALSE,
  .con = NULL
)
}
\arguments{
\item{df}{data.frame or tibble. Data returned by \code{caged_parse()}.}

\item{db_path}{character or NULL. Path to the file \code{.duckdb}.
Pode ser NULL se \code{.con} for fornecido.}

\item{table}{character or NULL. Name of the destination table. If NULL,
detecta automaticamente pela coluna \code{fonte_tipo} do data.frame.}

\item{overwrite_competencies}{logical. If TRUE, deletes records for the
competency before inserting (avoids duplicates on re-runs).
Default: FALSE.}

\item{.con}{a DBI connection object (optional). If provided, \code{db_path} is
ignored and the existing connection is reused. The caller is responsible
for closing the connection.}
}
\value{
invisible: number of rows inserted.
}
\description{
Usa \verb{INSERT OR IGNORE} por competência para evitar duplicatas:
se uma competência já existe na tabela, ela é pulada (útil para
re-rodar o pipeline sem apagar dados anteriores).
}
\details{
As tabelas criadas são:
\itemize{
\item \code{caged_mov}, \code{caged_for}, \code{caged_exc} --- para dados de 2020 em diante
\item \code{caged_antigo} --- para dados até 2019 (CAGED antigo)
\item \code{caged_ajustes} --- ajustes retroativos do CAGED antigo
}
}
\examples{
\dontrun{
# Parse e grava em um único fluxo (requer arquivo baixado)
df <- caged_parse("CAGEDMOV202301.7z")
caged_to_duckdb(df, db_path = file.path(tempdir(), "caged.duckdb"))

# Regravar uma competência já existente
caged_to_duckdb(df, db_path = file.path(tempdir(), "caged.duckdb"),
                overwrite_competencies = TRUE)
}

}
\seealso{
\code{\link[=caged_parse]{caged_parse()}} para gerar o data.frame de entrada.

\code{\link[=caged_load]{caged_load()}} para o pipeline completo.
}
