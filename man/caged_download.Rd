% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{caged_download}
\alias{caged_download}
\title{Baixa microdados do CAGED do repositório HuggingFace}
\usage{
caged_download(
  years,
  months = seq_len(12L),
  states = NULL,
  destdir = NULL,
  force = FALSE,
  timeout = 300,
  workers = getOption("datacaged.workers", min(3L, future::availableCores()))
)
}
\arguments{
\item{years}{integer vector. Desired years (ex: \code{2020:2023}).}

\item{months}{integer vector. Desired months (1–12). Default: \code{seq_len(12L)} (todos os meses).}

\item{states}{character vector or NULL. \strong{Ignorado} — mantido por compatibilidade.}

\item{destdir}{character or NULL. Local directory to save os arquivos.
If NULL, uses the default directory do sistema via
\code{tools::R_user_dir("datacaged", "cache")}.}

\item{force}{logical. If TRUE, re-downloads files already in cache.
Default: FALSE.}

\item{timeout}{integer. Timeout por arquivo em segundos. Default: 300.}

\item{workers}{integer. Number of parallel downloads. Default: 3.
Use \code{1} for sequential mode. Controlled via
\code{options(datacaged.workers = N)}.}
}
\value{
data.frame invisível com colunas \code{arquivo}, \code{competencia}, \code{type},
\code{uf} e \code{status} (baixado / cache / nao_encontrado / erro).
}
\description{
Baixa os arquivos \verb{.7z} para um diretório local, criando uma estrutura
de pastas por ano. Arquivos já baixados não são re-baixados (cache local).
}
\details{
Para o \strong{Novo CAGED} (2020+), são baixados três tipos por competência:
movimentações (\code{MOV}), fora do prazo (\code{FOR}) e exclusões (\code{EXC}).

Para o \strong{CAGED antigo} (até 2019), o download é nacional (arquivo único por competência).

\strong{Compatibilidade entre plataformas:}\tabular{llll}{
   Recurso \tab Windows \tab macOS \tab Linux \cr
   Download (HTTPS) \tab OK \tab OK \tab OK \cr
   Downloads paralelos \tab OK \tab OK \tab OK \cr
   Novo CAGED (2020+, LZMA) \tab OK \tab OK \tab OK \cr
   CAGED antigo (PPMd) \tab Requer 7-Zip \tab Requer 7-Zip \tab Requer 7-Zip ️ \cr
}


O CAGED antigo (pré-2020) usa PPMd e requer 7-Zip instalado.
O Novo CAGED usa LZMA e funciona em todas as plataformas sem dependências extras.
}
\examples{
\dontrun{
# Baixa Novo CAGED de jan-mar/2023
manifest <- caged_download(years = 2023, months = c(1L, 2L, 3L))

# Ver status de cada arquivo (baixado / cache / nao_encontrado / erro)
dplyr::count(manifest, status)

# Baixa CAGED antigo de 2018
caged_download(years = 2018)

# Forçar re-download mesmo com cache
caged_download(years = 2023, months = 1, force = TRUE)

# Salvar em diretório personalizado
caged_download(years = 2023, months = 1, destdir = file.path(tempdir(), "caged_cache"))
}

}
\seealso{
\code{\link[=caged_load]{caged_load()}} para o pipeline completo (download + parse + DuckDB).

\code{\link[=caged_hf_files]{caged_hf_files()}} para listar competências disponíveis no HuggingFace.
}
