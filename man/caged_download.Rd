% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\encoding{UTF-8}
\name{caged_download}
\alias{caged_download}
\title{Download CAGED microdata from HuggingFace}
\usage{
caged_download(
  years,
  months = seq_len(12L),
  states = NULL,
  destdir = NULL,
  force = FALSE,
  timeout = 300
)
}
\arguments{
\item{years}{integer vector. Desired years (ex: \code{2020:2023}).}

\item{months}{integer vector. Desired months (1-12).
Default: \code{seq\_len(12L)} (todos os meses).}

\item{states}{\strong{Ignorado} - o parametro e validado mas nao filtra downloads
de nenhuma serie (Novo CAGED, antigo ou Ajustes), pois todos os arquivos
sao de ambito nacional. Mantido por compatibilidade.}

\item{destdir}{character or NULL. Diretório local para salvar os arquivos.
Se NULL, usa o diretorio padrao do sistema via
\code{tools::R_user_dir("datacaged", "cache")} (localizacao varia por plataforma).}

\item{force}{logical. If TRUE, re-downloads files already in cache.
Default: FALSE.}

\item{timeout}{integer. Timeout per file in seconds. Default: 300.}
}
\value{
data.frame invisível com colunas \code{arquivo}, \code{competencia}, \code{type},
\code{uf} e \code{status} (baixado / cache / nao_encontrado / erro).
}
\description{
Baixa os arquivos \verb{.7z} para um diretório local, criando uma estrutura
de pastas por ano. Arquivos já baixados não são re-baixados (cache local).
}
\details{
Para o \strong{Novo CAGED} (2020+), são baixados três tipos por competência:
movimentações (\code{MOV}), fora do prazo (\code{FOR}) e exclusões (\code{EXC}).

Para o \strong{CAGED antigo} (até 2019), o download é nacional (arquivo único por competência).
}
\examples{
\donttest{
# Baixa Novo CAGED de jan-mar/2023
manifest <- caged_download(years = 2023, months = c(1L, 2L, 3L))

# Ver status de cada arquivo (baixado / cache / nao_encontrado / erro)
dplyr::count(manifest, status)

# Baixa CAGED antigo de 2018 (nacional, sem filtro por UF)
caged_download(years = 2018)

# Forçar re-download mesmo com cache
caged_download(years = 2023, months = 1, force = TRUE)

# Salvar em diretório personalizado
caged_download(years = 2023, months = 1, destdir = "D:/dados/caged")
}

}
\seealso{
\code{\link[=caged_load]{caged_load()}} for the full pipeline (download + parse + DuckDB).

\code{\link[=caged_hf_files]{caged_hf_files()}} to list available competencies no repositório HuggingFace.
}
