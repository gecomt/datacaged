% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{caged_load}
\alias{caged_load}
\title{Pipeline completo: download → parse → DuckDB}
\usage{
caged_load(
  years,
  months = seq_len(12L),
  states = NULL,
  db_path = file.path(tempdir(), "caged.duckdb"),
  destdir = NULL,
  force_download = FALSE,
  overwrite_competencies = FALSE,
  timeout = 300,
  workers = getOption("datacaged.workers", min(3L, future::availableCores()))
)
}
\arguments{
\item{years}{integer vector. Desired years.}

\item{months}{integer vector. Desired months (1–12). Default: \code{seq_len(12L)} (todos os meses).}

\item{states}{character vector or NULL. \strong{Ignored} — the parameter is validated
but does not filter downloads for any series (Novo CAGED, antigo or Ajustes),
as all files are national in scope. Kept for backwards compatibility.}

\item{db_path}{character. Path to the file \code{.duckdb}.
Default: \code{file.path(tempdir(), "caged.duckdb")}.}

\item{destdir}{character or NULL. Cache directory for \verb{.7z} files.}

\item{force_download}{logical. Re-downloads files already in cache.
Default: FALSE.}

\item{overwrite_competencies}{logical. Overwrites competencies already in the database.
Default: FALSE.}

\item{timeout}{integer. Timeout per file in seconds. Default: 300.}

\item{workers}{integer. Number of parallel downloads. Default: 3.
Use \code{1} for sequential mode. Controlled via
\code{options(datacaged.workers = N)}.}
}
\value{
invisible: tibble with final database statistics (via \code{caged_info()}).
}
\description{
Combina \code{caged_download()}, \code{caged_parse()} e \code{caged_to_duckdb()}
em um único comando. É o jeito mais simples de popular o banco local.
}
\examples{
\donttest{
# Novo CAGED 2022-2023
caged_load(
  years   = 2022:2023,
  db_path = file.path(tempdir(), "caged.duckdb")
)

# CAGED antigo nacional (2015-2019)
caged_load(
  years   = 2015:2019,
  db_path = file.path(tempdir(), "caged_historico.duckdb")
)

# Conecta e consulta depois
con <- caged_connect(file.path(tempdir(), "caged.duckdb"))
dplyr::tbl(con, "caged_mov") |>
  dplyr::group_by(competencia, uf) |>
  dplyr::summarise(saldo = sum(saldomovimentacao, na.rm = TRUE))
}

}
\seealso{
\code{\link[=caged_adjustments_load]{caged_adjustments_load()}} para o CAGED Ajustes (correções retroativas até 2019).

\code{\link[=caged_download]{caged_download()}} para baixar sem gravar no banco.

\code{\link[=caged_info]{caged_info()}} para inspecionar o banco após a carga.
}
