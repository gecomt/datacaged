% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\encoding{UTF-8}
\name{caged_load}
\alias{caged_load}
\title{Pipeline completo: download → parse → DuckDB}
\usage{
caged_load(
  years,
  months = seq_len(12L),
  states = NULL,
  db_path = file.path(tempdir(), "caged.duckdb"),
  destdir = NULL,
  force_download = FALSE,
  overwrite_competencies = FALSE,
  timeout = 300
)
}
\arguments{
\item{years}{integer vector. Desired years.}

\item{months}{integer vector. Desired months (1-12).
Default: \code{seq\_len(12L)} (todos os meses).}

\item{states}{\strong{Ignorado} - o parametro e validado mas nao filtra downloads
de nenhuma serie (Novo CAGED, antigo ou Ajustes), pois todos os arquivos
sao de ambito nacional. Mantido por compatibilidade.}

\item{db_path}{character. Path to the file \code{.duckdb}.
Default: \code{file.path(tempdir(), "caged.duckdb")}.}

\item{destdir}{character or NULL. Diretório de cache dos \verb{.7z}.}

\item{force_download}{logical. Re-baixa arquivos já em cache.
Default: FALSE.}

\item{overwrite_competencies}{logical. Regrava competências já no banco.
Default: FALSE.}

\item{timeout}{integer. Timeout per file in seconds. Default: 300.}
}
\value{
invisible: tibble com estatísticas finais do banco (via \code{caged_info()}).
}
\description{
Combina \code{caged_download()}, \code{caged_parse()} e \code{caged_to_duckdb()}
em um único comando. É o jeito mais simples de popular o banco local.
}
\examples{
\donttest{
# Novo CAGED SP e RJ, 2022-2023
caged_load(
  years   = 2022:2023,
  db_path = file.path(tempdir(), "caged.duckdb")
)

# CAGED antigo nacional (2015-2019)
caged_load(
  years   = 2015:2019,
  db_path = file.path(tempdir(), "caged_historico.duckdb")
)

# Conecta e consulta depois
con <- caged_connect(file.path(tempdir(), "caged.duckdb"))
dplyr::tbl(con, "caged_mov") |>
  dplyr::group_by(competencia, uf) |>
  dplyr::summarise(saldo = sum(saldomovimentacao, na.rm = TRUE))
}

}
\seealso{
\code{\link[=caged_adjustments_load]{caged_adjustments_load()}} para o CAGED Ajustes
(correções retroativas até 2019).

\code{\link[=caged_download]{caged_download()}} para baixar sem gravar no banco.

\code{\link[=caged_info]{caged_info()}} to inspect the database após a carga.
}
